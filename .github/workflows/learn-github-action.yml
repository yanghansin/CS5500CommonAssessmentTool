name: cs5500PIPLINE

defaults:
  run:
    shell: bash


on: [push]
jobs:
  run-tests:
    runs-on: macos-latest
    steps:
    - run: echo "🎉 Hello World!"
    - run: echo "🐧"
    - run: echo "🔎"

